pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "building the build"
            }
        }
        stage('Test') {
            steps {
                echo "testing the test"
            }
        }
        stage('Deploy') {
            steps {
                echo " deploying the code "
            }
        }
    }
}
